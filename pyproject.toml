[tool.ruff]
target-version = "py311"

[tool.ruff.lint]
# Incremental correctness baseline. The initial full default scan found 263
# existing findings; architecture slices must not mix in mass style cleanup.
select = ["E9", "F63", "F7", "F82"]
